TimeTracker project:
This project is an electron application made for efficiently tracking time and logging daily activities on windows, mac and linux.
The save data goes to the user's AppData directory.

To install it, you have to have npm and nodejs installed.
Then, run the command "npm install" to install the dependencies of the project.
Finally, run the code with "npm start" (or "npm electron .")
